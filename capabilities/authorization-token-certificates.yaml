naftiko: 1.0.0-alpha2
info:
  label: Coveo Authorization Server API — Token Certificates
  description: 'Coveo Authorization Server API — Token Certificates. 1 operations. Lead operation: Token Certificates. Self-contained
    Naftiko capability covering one Coveo business surface.'
  tags:
  - Coveo
  - Authorization Server
  - Token Certificates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    COVEO_API_KEY: COVEO_API_KEY
capability:
  consumes:
  - type: http
    namespace: authorization-token-certificates
    baseUri: https://platform.cloud.coveo.com
    description: Coveo Authorization Server API — Token Certificates business capability. Self-contained, no shared references.
    authentication:
      type: bearer
      token: '{{env.COVEO_API_KEY}}'
    resources:
    - name: rest-tokens-certificates
      path: /rest/tokens/certificates
      operations:
      - name: getpubliccertificates
        method: GET
        description: ''
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: authorization-token-certificates-rest
    port: 8080
    description: REST adapter for Coveo Authorization Server API — Token Certificates. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/tokens/certificates
      name: rest-tokens-certificates
      description: REST surface for rest-tokens-certificates.
      operations:
      - method: GET
        name: getpubliccertificates
        description: getpubliccertificates
        call: authorization-token-certificates.getpubliccertificates
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: authorization-token-certificates-mcp
    port: 9090
    transport: http
    description: MCP adapter for Coveo Authorization Server API — Token Certificates. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: getpubliccertificates
      description: getpubliccertificates
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: authorization-token-certificates.getpubliccertificates
      outputParameters:
      - type: object
        mapping: $.
